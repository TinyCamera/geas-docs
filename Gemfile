source "https://rubygems.org"

gem "jekyll", "~> 4.3"
gem "jekyll-remote-theme"
gem "jekyll-seo-tag"
gem "jekyll-include-cache"

# Just the Docs — the remote theme pulls itself; this dep is only needed if
# someone wants to build locally. Harmless on GitHub Pages.
gem "just-the-docs"
